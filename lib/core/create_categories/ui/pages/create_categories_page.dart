import 'package:flutter/material.dart';

class CreateCategoriesPage extends StatelessWidget {
  const CreateCategoriesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.grey.shade100,
      body: const Column(
        children: [
          
        ],
      ),
    );
  }
}